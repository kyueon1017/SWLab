#include <iostream>
#include <cmath>
#include <fstream>

using namespace std;

/*
int main() {
	ofstream file("myfile2.txt"); 

	if (file.is_open()) {
		for (int n = 0; n <= 10; n++) {
			file << n << " "<< pow(2, n) << endl; // txt파일에 출력
			cout << n << " "<< pow(2, n) << endl; // vs studio화면에 출력 
		}
		file.close();
	}
	
	else {
		cout << "error! 파일을 열 수 없습니다\n";
		return -1;
	}

	return 0; 
}

*/

